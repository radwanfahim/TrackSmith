import AuthCard from "./AuthCard";

const Auth = () => {
  return (
    <section class=" bg-linear-to-br from-teal-50 via-white to-cyan-50 min-h-screen relative">
      <div class="container mx-auto custom-container ">
        <AuthCard />
      </div>
    </section>
  );
};

export default Auth;
