import AuthCard from "../../../ui/AuthCard";

const Auth = () => {
  return (
    <section class=" bg-linear-to-br from-teal-50 via-white to-cyan-50 min-h-screen">
      <AuthCard />
    </section>
  );
};

export default Auth;
